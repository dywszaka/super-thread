# Electron's installer downloads its runtime separately from the npm registry.
# Use a reachable mirror so a normal `npm install` works on restricted networks.
electron_mirror=https://npmmirror.com/mirrors/electron/
